type Props = {
	id: string;
};

export const XAxis = ({}: Props) => {
	return <div>HELLO WORLD X AXIS</div>;
};

// XAxis.context = (ctx: GraphContext, props: Props) => {
// 	props.id;
// 	return ctx;
// };
